@model AdminLearningItemListViewModel
@{
    ViewData["Title"] = "Kho ná»™i dung";
}

<section class="container dashboard-wrap">
    @if (TempData["AdminMessage"] is string message)
    {
        <div class="feedback-band success"><span class="material-symbols-outlined">check_circle</span><strong>@message</strong></div>
    }
    <div class="dashboard-header">
        <div>
            <p class="eyebrow">Quáº£n trá»‹ ná»™i dung</p>
            <h1>Kho bÃ i há»c</h1>
        </div>
        <div class="hero-actions">
            <a class="app-btn app-btn-muted app-btn-small" asp-action="Catalogs">
                <span class="material-symbols-outlined">account_tree</span>
                Cáº¥u trÃºc chÆ°Æ¡ng trÃ¬nh
            </a>
            <a class="app-btn app-btn-small" asp-action="CreateChoice" asp-route-skillGroupId="@Model.SkillGroupId" asp-route-topicId="@Model.TopicId">
                <span class="material-symbols-outlined">add</span>
                Táº¡o bÃ i tÆ°Æ¡ng tÃ¡c
            </a>
            <a class="app-btn app-btn-small" asp-action="CreateTracing" asp-route-skillGroupId="@Model.SkillGroupId" asp-route-topicId="@Model.TopicId">
                <span class="material-symbols-outlined">draw</span>
                Táº¡o bÃ i tÃ´ nÃ©t
            </a>
            <a class="app-btn app-btn-muted app-btn-small" asp-action="Index">Dashboard</a>
        </div>
    </div>

    <form class="soft-card admin-filter" asp-action="LearningItems" method="get">
        <div>
            <label for="skillGroupId">NhÃ³m ká»¹ nÄƒng</label>
            <select id="skillGroupId" name="skillGroupId" class="form-control">
                <option value="">Táº¥t cáº£</option>
                @foreach (var group in Model.SkillGroups)
                {
                    <option value="@group.Id" selected="@(Model.SkillGroupId == group.Id)">@group.Name</option>
                }
            </select>
        </div>
        <div>
            <label for="status">Tráº¡ng thÃ¡i</label>
            <select id="status" name="status" class="form-control">
                <option value="">Táº¥t cáº£</option>
                <option value="@ContentStatus.Draft" selected="@(Model.Status == ContentStatus.Draft)">NhÃ¡p</option>
                <option value="@ContentStatus.Review" selected="@(Model.Status == ContentStatus.Review)">Chá» duyá»‡t</option>
                <option value="@ContentStatus.Published" selected="@(Model.Status == ContentStatus.Published)">Xuáº¥t báº£n</option>
                <option value="@ContentStatus.Archived" selected="@(Model.Status == ContentStatus.Archived)">LÆ°u trá»¯</option>
            </select>
        </div>
        <div>
            <label for="topicId">Chủ đề</label>
            <select id="topicId" name="topicId" class="form-control">
                <option value="">Tất cả</option>
                @foreach (var topic in Model.Topics.Where(x => !Model.SkillGroupId.HasValue || x.SkillGroupId == Model.SkillGroupId))
                {
                    <option value="@topic.Id" selected="@(Model.TopicId == topic.Id)">@topic.Name</option>
                }
            </select>
        </div>
        <div>
            <label for="interactionType">Dạng bài</label>
            <select id="interactionType" name="interactionType" class="form-control">
                <option value="">Táº¥t cáº£</option>
                <option value="@InteractionTypes.SingleChoice" selected="@(Model.InteractionType == InteractionTypes.SingleChoice)">Chá»n Ä‘Ã¡p Ã¡n</option>
                <option value="@InteractionTypes.MultiSelect" selected="@(Model.InteractionType == InteractionTypes.MultiSelect)">Chá»n nhiá»u</option>
                <option value="@InteractionTypes.ListenAndChoose" selected="@(Model.InteractionType == InteractionTypes.ListenAndChoose)">Nghe - chá»n</option>
                <option value="@InteractionTypes.DragDrop" selected="@(Model.InteractionType == InteractionTypes.DragDrop)">KÃ©o - tháº£</option>
                <option value="@InteractionTypes.Matching" selected="@(Model.InteractionType == InteractionTypes.Matching)">Ná»‘i cáº·p</option>
                <option value="@InteractionTypes.Ordering" selected="@(Model.InteractionType == InteractionTypes.Ordering)">Sáº¯p xáº¿p</option>
                <option value="@InteractionTypes.Counting" selected="@(Model.InteractionType == InteractionTypes.Counting)">Äáº¿m Ä‘á»“ váº­t</option>
                <option value="@InteractionTypes.QuantityBuilder" selected="@(Model.InteractionType == InteractionTypes.QuantityBuilder)">Táº¡o sá»‘ lÆ°á»£ng</option>
                <option value="@InteractionTypes.Comparison" selected="@(Model.InteractionType == InteractionTypes.Comparison)">So sÃ¡nh</option>
                <option value="@InteractionTypes.Classification" selected="@(Model.InteractionType == InteractionTypes.Classification)">PhÃ¢n loáº¡i</option>
                <option value="@InteractionTypes.StoryChoice" selected="@(Model.InteractionType == InteractionTypes.StoryChoice)">Nghe truyá»‡n</option>
                <option value="@InteractionTypes.Tracing" selected="@(Model.InteractionType == InteractionTypes.Tracing)">TÃ´ nÃ©t</option>
            </select>
        </div>
        <button class="app-btn app-btn-small" type="submit">Lá»c</button>
    </form>

    <div class="soft-card report-panel mt-4">
        <table class="app-table admin-content-table">
            <thead>
            <tr>
                <th>TÃªn bÃ i</th>
                <th>NhÃ³m</th>
                <th>Dáº¡ng</th>
                <th>Thá»© tá»±</th>
                <th>CÃ¢u há»i</th>
                <th>Tráº¡ng thÃ¡i</th>
                <th></th>
            </tr>
            </thead>
            <tbody>
            @foreach (var item in Model.Items)
            {
                var isCompatible = ActivityTemplateCatalog.IsItemAllowed(item);
                <tr>
                    <td><a class="table-link" asp-action="LearningItemDetail" asp-route-id="@item.Id">@item.Title</a></td>
                    <td>@item.SkillGroup?.Name</td>
                    <td>@ActivityTemplateCatalog.GetDisplayName(item.InteractionType) @if (!isCompatible) { <span class="status-chip status-warning">Cáº§n chuáº©n hÃ³a</span> }</td>
                    <td>@item.SortOrder</td>
                    <td>@item.Questions.Count</td>
                    <td><span class="status-chip status-@item.Status">@ContentStatus.GetDisplayName(item.Status)</span></td>
                    <td class="table-actions">
                        @if (item.InteractionType == InteractionTypes.Tracing)
                        {
                            <a class="mini-action" asp-action="CreateTracing" asp-route-editId="@item.Id">Sá»­a</a>
                        }
                        else
                        {
                            <a class="mini-action" asp-action="CreateChoice" asp-route-editId="@item.Id">Sá»­a</a>
                        }
                        <form class="inline-action-form" asp-action="DeleteLearningItem" asp-route-id="@item.Id" method="post" data-confirm-delete>
                            <button class="mini-action danger-link" type="submit">XÃ³a</button>
                        </form>
                    </td>
                </tr>
            }
            </tbody>
        </table>
    </div>

    @if (Model.TotalPages > 1)
    {
        <nav class="admin-pagination" aria-label="PhÃ¢n trang kho bÃ i há»c">
            <a class="icon-button @(Model.Page == 1 ? "disabled" : "")"
               asp-action="LearningItems" asp-route-page="@(Model.Page - 1)" asp-route-status="@Model.Status"
               asp-route-interactionType="@Model.InteractionType" asp-route-skillGroupId="@Model.SkillGroupId" asp-route-topicId="@Model.TopicId"
               aria-label="Trang trÆ°á»›c"><span class="material-symbols-outlined">chevron_left</span></a>
            <strong>Trang @Model.Page / @Model.TotalPages</strong>
            <span>@Model.TotalItems bÃ i há»c</span>
            <a class="icon-button @(Model.Page == Model.TotalPages ? "disabled" : "")"
               asp-action="LearningItems" asp-route-page="@(Model.Page + 1)" asp-route-status="@Model.Status"
               asp-route-interactionType="@Model.InteractionType" asp-route-skillGroupId="@Model.SkillGroupId" asp-route-topicId="@Model.TopicId"
               aria-label="Trang sau"><span class="material-symbols-outlined">chevron_right</span></a>
        </nav>
    }
</section>
